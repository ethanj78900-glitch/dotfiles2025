# vim: set ft=zsh :

if [ -n "$__EXECUTED_GIT_COMMAND" ]; then
    update_current_git_vars
    unset __EXECUTED_GIT_COMMAND
fi

if [ -n "$__EXECUTED_EDITOR_COMMAND" ]; then
    update_current_git_vars
    unset __EXECUTED_EDITOR_COMMAND
fi
